services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: agent_space
      POSTGRES_USER: agent_space
      POSTGRES_PASSWORD: agent_space
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
